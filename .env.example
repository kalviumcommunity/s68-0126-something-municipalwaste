# Example environment variables for the application, replace the values with your own as required.

# Server-side variables
DATABASE_URL=postgres://<username>:<password>@localhost:5432/<dbname>
SECRET_KEY=your_secret_key_here # Generate a new secret with: openssl rand -hex 32

# NextAuth Configuration
# Generate a random secret using: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-here
NEXTAUTH_URL=http://localhost:3000

# GitHub OAuth
# Create a GitHub OAuth App at: https://github.com/settings/developers
# Set Authorization callback URL to: http://localhost:3000/api/auth/callback/github
GITHUB_ID=your-github-client-id
GITHUB_SECRET=your-github-client-secret

# Client-side variables (must start with NEXT_PUBLIC_)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
